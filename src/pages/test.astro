---
import MainLayout from '../../../layouts/main.astro';
import { getCollection } from 'astro:content';

try {
  const allPosts = await getCollection('blog');
  console.log('Successfully loaded blog collection:', allPosts.length);
} catch (error) {
  console.error('Error loading blog collection:', error);
}
---

<MainLayout title="Test Page - Lit Phansiri">
  <div class="container mx-auto px-4 py-16">
    <h1 class="text-4xl font-bold mb-8">Test Page</h1>
    <p>This is a test page to check if the blog collection loads correctly.</p>
  </div>
</MainLayout>
